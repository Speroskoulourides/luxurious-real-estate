name: Fix ES Company geometry

on:
  push:
    paths:
      - '.github/workflows/fix-es-company-geometry.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply ES Company geometry
        run: python3 .github/scripts/fix_es_company_geometry.py
      - name: Verify and commit
        run: |
          grep -F '#company{min-height:calc(100vh - 76px)!important;scroll-margin-top:0!important}' es/index.html
          test "$(git diff --name-only)" = "es/index.html"
          git diff --check
          git config user.name 'SNK ES Company geometry'
          git config user.email 'actions@users.noreply.github.com'
          git add es/index.html
          git commit -m 'Fix ES Company geometry'
          git push origin HEAD:main
