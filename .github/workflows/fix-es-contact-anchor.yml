name: Fix ES Contact anchor offset

on:
  push:
    paths:
      - '.github/workflows/fix-es-contact-anchor.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply ES Contact anchor fix
        run: python3 .github/scripts/fix_es_contact_anchor.py
      - name: Verify and commit
        run: |
          grep -F '#contact{scroll-margin-top:0!important}' es/index.html
          test "$(git diff --name-only)" = "es/index.html"
          git diff --check
          git config user.name 'SNK ES Contact anchor'
          git config user.email 'actions@users.noreply.github.com'
          git add es/index.html
          git commit -m 'Fix ES Contact anchor offset'
          git push origin HEAD:main
