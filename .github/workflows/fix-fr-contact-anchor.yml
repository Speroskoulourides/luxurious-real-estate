name: Fix FR Contact anchor offset

on:
  push:
    paths:
      - '.github/workflows/fix-fr-contact-anchor.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply FR Contact anchor
        run: python3 .github/scripts/fix_fr_contact_anchor.py
      - name: Verify and commit
        run: |
          grep -F '#contact{scroll-margin-top:0!important}' fr/index.html
          test "$(git diff --name-only)" = "fr/index.html"
          git diff --check
          git config user.name 'SNK FR Contact anchor'
          git config user.email 'actions@users.noreply.github.com'
          git add fr/index.html
          git commit -m 'Fix FR Contact anchor offset'
          git push origin HEAD:main
